import Dashboard from "./Dashboard";

const Index = () => {
  return <Dashboard characters={undefined as any} campaigns={undefined as any} isAuthenticated={true} userName="Agente Viktor" />;
};

export default Index;
